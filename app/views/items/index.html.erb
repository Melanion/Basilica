<p id="notice"><%= notice %></p>

<table>
  <tr>
  	<th colspan="3"><h1><%= Basilica %></h1></th>
  </tr>
  <tr>
  	<th>&nbsp;</th>
    <th>Item</th>
    <th>Quantity</th>
  </tr>
<% @items.each do |item| %>
  <tr>
  	<td style="width:10%;"><%= image_tag(item.img_url, class: 'list_image') %></td>
    <td><%= item.name %></td>
    <td><%= item.quantity %></td>
  </tr>
<% end %>
</table>
